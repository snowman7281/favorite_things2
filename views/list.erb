<% if @error != [] || @error != '[#]' %>
  <p> <%= @error %> </p>
<% end %>
<% @list.each do |item| %>
   <h4>Rank Number: <a href="/items/<%= item.id %>"><%= item.rank %></a>&nbsp&nbsp&nbspItem Name: <a href="/items/<%= item.id %>"><%= item.name %></a></h4>
   <h4></h4>
 <% end %>

<!-- # <% @list.each do |item| %>
#   <h4><a href="/items/<%= item.id %>"><%= item.rank %></a></h4>
# <% end %> -->

<h4>Add an item to the list:</h4>

<form action="/" method="post">
  <div class="form-group">
    <label for="name">Item Name</label>
    <input id="name" name="name" class="form-control" type="text">
  </div>
  <div class="form-group">
    <label for="rank">Rank Number</label>
    <input id="rank" name="rank" class="form-control" type="text">
  </div>
  <button type="submit" id="submit" class="btn">submit</button>
</form>

<a href="/">clear</a>
